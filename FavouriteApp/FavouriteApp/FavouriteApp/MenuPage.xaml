<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FavouriteApp"
             Title="FlyoutMenuPage"
             x:Class="FavouriteApp.MenuPage">

    <StackLayout>
        <ListView x:Name="view" x:FieldModifier="public">
            <ListView.ItemsSource>
                <x:Array Type="{x:Type local:Flyoutitem}">
                    <local:Flyoutitem Title="Home" Navpages="{x:Type local:Home}"/>
                    <local:Flyoutitem Title="Movies" Navpages="{x:Type local:Movies}"/>
                    <local:Flyoutitem Title="Cinemas" Navpages="{x:Type local:Cinemas}"/>
                    <local:Flyoutitem Title="Purchases" Navpages="{x:Type local:Purchases}"/>
                    <local:Flyoutitem Title="Messages" Navpages="{x:Type local:Messages}"/>
                </x:Array>
            </ListView.ItemsSource>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Label Text="{Binding Title}"/>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
     

</ContentPage>